// Importing React (optional in newer versions of React, but still commonly done)
import React from 'react'

// Functional component named HelloWorld
// Arrow function syntax is used here
const HelloWorld = () => {
  return (
    <div>
      HelloWorld
    </div>
  )
}

// Exporting the component so it can be imported and used in other files
export default HelloWorld
